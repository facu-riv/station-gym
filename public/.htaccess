RewriteEngine On

# Si es un archivo o directorio real, servirlo directamente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule . - [L]

# Cualquier otra cosa al index.php
RewriteRule ^(.*)$ index.php [QSA,L]